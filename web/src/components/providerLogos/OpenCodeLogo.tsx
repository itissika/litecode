/**
 * OpenCode brand mark — monochrome silhouette.
 * Tinted via fill="currentColor", so it inherits the surrounding text
 * color (theme tokens) automatically.
 *
 * Artwork: lobe-icons (MIT).
 */
export function OpenCodeLogo() {
  return (
    <svg
      viewBox="0 0 24 24"
      fill="currentColor"
      fillRule="evenodd"
      aria-hidden="true"
      className="block h-full w-full"
    >
      <path d="M16 6H8v12h8V6zm4 16H4V2h16v20z" />
    </svg>
  );
}
