# ── LiteCode optional override variables ────────────────────────────
# Copy this file to `.env` and fill in values as needed.
# `.env` is git-ignored; never commit real secrets.

# MCP SQLite Server (optional, for eval/retrieval pipelines only).
# Leave unset to use the default per-workspace `.litecode/sessions.db`.
SQLITE_DB_PATH=
